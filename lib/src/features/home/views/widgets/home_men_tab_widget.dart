import 'package:flutter/material.dart';

class HomeMenTabWidget extends StatelessWidget {
  const HomeMenTabWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Search"));
  }
}
